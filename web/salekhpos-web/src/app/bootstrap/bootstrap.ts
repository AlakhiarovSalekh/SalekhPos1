/**
 * Application bootstrap. Reserved for one-time setup (feature flags,
 * telemetry, error reporting) once they are introduced. Empty in the
 * foundation scaffold.
 */
export function bootstrap(): void {
  // Intentionally empty. Concrete bootstrap steps are added in their
  // respective phases.
}
